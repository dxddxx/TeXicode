#!/usr/bin/env bash
# Resolve script directory in a portable way
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# if piped input is detected, use the whole input as a single argument
# else use arg input as separate arguments
# [ ! -t 0 ] && piped_input="$(cat)"
# if [[ -n "$piped_input" ]]; then
#     python3 "$SCRIPT_DIR/src/main.py" "$piped_input"
# else
#     python3 "$SCRIPT_DIR/src/main.py" "$@"
# fi

# if [ -z "$1" ]; then
#     echo "Usage: $0 <input_markdown_file>"
#     exit 1
# fi
# 
# input_file="$1"

# Run the package module with src on PYTHONPATH so local checkout works
PYTHONPATH="$SCRIPT_DIR/src" PYTHONNOUSERSITE=1 python3 -c "import sys; sys.path.insert(0, '$SCRIPT_DIR/src'); from texicode.main import main; main()" "$@"
